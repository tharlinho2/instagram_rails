<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta charset="UTF-8" />

    <title>Autenticação - Instagram</title>
    <%= csrf_meta_tags %> 
    <%= csp_meta_tag %>
    
    <%= stylesheet_pack_tag "authentication" %> 
    <%= javascript_pack_tag "authentication" %>
  </head>

  <body data-flash-messages="<%= JSON.dump(flash.to_a) %>" >

    <main>
        <section>
            <div>
                <%= image_tag "instagram.png" %>
            </div>

            <%= yield %>
        </section>
    </main>
  </body>
</html>
